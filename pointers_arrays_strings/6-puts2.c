#include <stdio.h>
#include "main.h"

/**
 * puts2 - entry point*
 * @str: pointer to an string
 * Description: function that prints every other character of a string.
 * Return: nothing
 */

void puts2(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		if (i % 2 == 0)
sh: 1: q: not found
sh: 1: q: not found
	putchar('\n');
}
